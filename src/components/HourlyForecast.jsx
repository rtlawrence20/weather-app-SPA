export default function HourlyForecast({ weather }) {
  // Placeholder until API integration is done
  return (
    <div className="panel">
      <h3>Hourly Forecast (placeholder)</h3>
      <p>
        Once the API is wired, this will show the next 12–24 hours of temperature,
        precipitation, and icons.
      </p>
    </div>
  );
}
